# Read about fixtures at https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html
#
# No fixture rows defined: Spree::AdminUser requires a unique, present email,
# and no test in this suite relies on named admin_user fixtures. Tests that
# need an admin user should create one explicitly.
