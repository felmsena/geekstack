# Read about fixtures at https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html
#
# No fixture rows defined: Spree::User requires a unique, present email,
# and no test in this suite relies on named user fixtures. Tests that need
# a user should create one explicitly.
